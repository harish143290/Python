
Types Of parameters in Python Functions:
    1.Positional Parameters | arguments
              1.By default all formal parameters
              are works as positional arguments.

              2.If we define any function with positional
              arguments then we must pass same no.of.
              arguments by the time of calling that function.

              3.In this case order of the parameter are imp.
              
    2.Named Parameter | keyword arguments
            1.By default all positional parameters | formal
            parameter also works keyword arguments.

            2.It is process of passing the values formal
            parameter | arguments by using their names.

            3.In this case order of arguments is not imp.

    
    3.Default arguments
          1.It is the process of defining the formal arguments
          with some values.

          2.The default values are replaced by the actual
          arguments.

          3.the default values are taken by PVM whenever
          we fail to pass the values to the formal parameter.

          4.default arguments are acts as optional arguments.
       
    4.Positional only parameters | arguments
         1.If we declare any formal arguments left to the
         / formater then those parameter | arguments are
         acts positional only arguments.

         2.In this case order is very imp.

     5. keyword only arguments.
          1.If we declare any formal arguments right side of
          * arguments are acts keyword only arguments.

          2.In this case we must pass the values to arguments
          by using their names only.         
         
             
    6.varargs parameters
          * varargs means variable length arguments.

          * if any variable which declare by using *
          then those parameter acts varargs parameters.

          * varargs arguments will take 0-arguments to
          N no.of.arguments.thus varargs parameter will
          also work like an optional parameter.

          * varargs can take n no.of. positional only
             arguments.
                
    7.kwargs parameters
          * if any argument which is declared by using **
          then those arguments will works as kwargs
          parameter.

          * kwargs can take n no.of. keyword only arguments.

          * kwargs internally works as dict collection.

          * Based on the application req we can also
          declare **kwargs along with other type of parameter
          but in this case **kwargs should be last the
          arguments.

          



          
  
    











