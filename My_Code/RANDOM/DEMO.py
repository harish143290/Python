

random module
  - Used to generate random numbers
  - generate random items from the list
  - We generate OTP | CAPTCHA
  
  random()
     - will generate random float numbers between 0 and 1
     where 1 is not included.

  randint(a,b):
    - it will generate random int numbers between a and b
    where a and b are inlcuded.
  
  uniform(a,b):
      - it will generate random float numbers between a and b

  choice(seq) -> item | str
     - it will generate random item from given sequence

   range(start,stop,step) -> range object
   randrange(start,stop[,step] ) -> number

   

      

     
